# Liquid Glass patterns

Scroll-minimized tab bar, cooking mode toolbar with timer, glass context menu for meal status, composed from FitMeal components on 375 × 780 iPhone frames. A showcase page, not a component: build the SwiftUI screen from the components it uses.
