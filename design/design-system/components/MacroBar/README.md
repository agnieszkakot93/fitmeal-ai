# MacroBar

A labelled progress bar for one macro against its target, with a check when met.

**Props:** `macro` (`protein` | `carbs` | `fat`), `value`, `target` (grams), `mode` (`min` shows ≥ and counts as met once reached — use for protein minimums), `compact`.
