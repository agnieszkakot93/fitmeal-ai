# DistributionEditor

Shows and edits how the day's calories split across meals, as a stacked bar plus per-meal steppers.

**Props:** `kcal` (daily target), `meals` — `[{name, pct}]` summing to 100. Steppers move 1%; the last meal absorbs the difference.
