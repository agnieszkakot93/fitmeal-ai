# SegmentedControl

A one-of-N switch for short, parallel options: meals per day, plan length, Simple/Advanced macros.

**Props:** `options` (strings or `{value, label}`), `value`, `onChange(value)`, `label` (for the radiogroup). Two to five options, labels of one or two words.
