# ChangeItem

One line of *Why did FitMeal change this?* — the change and its measured effect.

**Props:** `what` (*Reduced oil by 8 g*), `effect` (*saves 72 kcal*), `icon`. Render inside `<ul class="fm-changes">`.

- Effects come from the nutrition engine, never from the LLM. Quote numbers, not adjectives.
