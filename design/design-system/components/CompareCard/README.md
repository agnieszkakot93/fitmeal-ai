# CompareCard

Original vs Your version — the moment FitMeal shows what it did to a recipe.

**Props:** `original`, `yours` (`{kcal, protein}`), `target` (text, *520 kcal · 45+ g protein*), `found` (false hides the *Optimized version found* pill), labels.
