# PhoneFrame

An iPhone-sized frame (375 × 780) for showcase pages: status bar, screen, optional footer, home indicator.

**Props:** `caption`, `children` (the screen content), `footer` (a `TabBar` or sticky CTA), `theme` (force `light` or `dark`). Showcase only — never ship it in the app.
