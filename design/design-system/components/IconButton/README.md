# IconButton

A 44px round icon-only button for nav bars and steppers.

**Props:** `icon`, `label` (required — becomes `aria-label`), `variant` (`sunken` for 32px steppers, `raised` for over imagery), `size`, `onClick`, `disabled`.
