# Plan states — no plan, relaxed goals, offline

No valid plan (what blocked it and what to change), a plan with a relaxed soft goal, and offline behaviour, composed from FitMeal components on 375 × 780 iPhone frames. A showcase page, not a component: build the SwiftUI screen from the components it uses.
