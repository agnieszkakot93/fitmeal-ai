# Onboarding 1 — welcome, age, consent, goal

Welcome, 18+ confirmation (and the under-18 stop), health-data consent with the health notice, Goal, composed from FitMeal components on 375 × 780 iPhone frames. A showcase page, not a component: build the SwiftUI screen from the components it uses.
