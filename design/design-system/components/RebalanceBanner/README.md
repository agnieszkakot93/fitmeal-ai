# RebalanceBanner

The one-tap fix after a swap pushes the day off target: *Adjust dinner portion? −70 kcal*.

**Props:** `title`, `body`, `before` / `after` (`{kcal, protein}`), `cta`.

- Always shows both totals so the user sees what they approve.
- Never auto-apply; *Keep as is* must be one tap.
