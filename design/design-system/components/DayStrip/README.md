# DayStrip

The week selector at the top of Plan: day name, date and a status dot.

**Props:** `days` — `[{name, num, state: 'planned'|'done'|'prep'}]`, `selected` (index). `done` = every meal eaten (basil), `prep` = a cooking day for meal prep (paprika).
