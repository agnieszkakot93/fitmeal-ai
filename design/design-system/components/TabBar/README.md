# TabBar

The five-tab bar: Today · Plan · Add · Shopping · Profile, with Add as a paprika button.

**Props:** `active` (`today` | `plan` | `shopping` | `profile`). Add opens the Add Recipe sheet; it is never "active".
