# Badge

A small label for state: meal-prep portion, prepare fresh, allergy, low confidence, plan tier.

**Props:** `tone` — `neutral`, `basil` (on target, best fit), `paprika` (changed by FitMeal), `warning` (intolerance, check this), `danger` (allergy), `premium`, `solid`; `icon`; children = one to three words.
