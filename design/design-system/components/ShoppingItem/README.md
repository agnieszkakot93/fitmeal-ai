# ShoppingItem

One aggregated line of the shopping list: name, total, package count, pantry subtraction.

**Props:** `name`, `qty` (*1.2 kg*), `packages` (*4 × 300 g cup*), `pantry` (*300 g at home*), `price` (*~14 PLN*), `checked`. Render inside `.fm-group` under a `SectionHeader` per category.
